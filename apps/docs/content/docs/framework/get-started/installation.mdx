---
title: Installation
description: Install ADK TypeScript and set up your development environment
---

import { Steps } from 'fumadocs-ui/components/steps';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';

## Prerequisites

<Callout type="info" title="System Requirements">
- **Node.js**: v22.0 or higher
- **Package Manager**: npm, yarn, or pnpm
- **TypeScript**: v5.3 or higher (optional but recommended)
</Callout>

## Recommended: CLI Installation

The fastest way to get started is using our CLI tool:

```bash
npx @iqai/adk-cli new
```

Or install globally:

```bash
npm install -g @iqai/adk-cli
adk new
```

This creates a complete project with examples, TypeScript configuration, and all dependencies.

<Callout type="info" title="Ready to Build?">
After installation, head to the [Quickstart Guide](/docs/framework/get-started/quickstart) to create your first agent and learn the fundamentals.
</Callout>

## Add to Existing Project

To add ADK to an existing TypeScript project:

<Tabs items={['npm', 'yarn', 'pnpm']}>
  <Tab value="npm">
    ```bash
    npm install @iqai/adk
    ```
  </Tab>

  <Tab value="yarn">
    ```bash
    yarn add @iqai/adk
    ```
  </Tab>

  <Tab value="pnpm">
    ```bash
    pnpm add @iqai/adk
    ```
  </Tab>
</Tabs>

<Callout type="info" title="Complete Setup Guide">
For step-by-step project setup instructions, environment configuration, and your first agent, see the [Quickstart Guide](/docs/framework/get-started/quickstart).
</Callout>

### Quick Test

Verify the installation works:

```typescript
import { AgentBuilder } from "@iqai/adk";

const response = await AgentBuilder
  .withModel("gemini-2.5-flash")
  .ask("Hello! Installation working?");

console.log("✅ Response:", response);
```



## Troubleshooting

### Common Installation Issues

<Callout type="warn" title="Node.js Version">
If you encounter installation errors, ensure you're using Node.js v22.0 or higher:
```bash
node --version
```
</Callout>

<Callout type="warn" title="TypeScript Compatibility">
For TypeScript projects, ensure you're using v5.3 or higher:
```bash
npx tsc --version
```
</Callout>

### Getting Help

If you encounter issues:

1. **Check the examples**: Our [examples](https://github.com/IQAIcom/adk-ts/tree/main/apps/examples/src) directory contains working setups
2. **Review error messages**: Most installation issues are dependency-related
3. **Open an issue**: Report bugs or compatibility issues on GitHub

## Next Steps

Now that ADK is installed:

1. **[Build your first agent](/docs/framework/get-started/quickstart)** - Complete project setup and create your first agent
2. **[Explore examples](https://github.com/IQAIcom/adk-ts/tree/main/apps/examples)** - See real working implementations
3. **[Learn core concepts](/docs/framework/agents)** - Understand agents, tools, and workflows

<Callout type="info" title="Start Here">
Head to the **[Quickstart Guide](/docs/framework/get-started/quickstart)** for your complete setup walkthrough and first agent!
</Callout>
